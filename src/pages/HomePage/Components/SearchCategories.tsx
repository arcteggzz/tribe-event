import styles from "../Styles/SearchCategories.module.scss";

const SearchCategories = () => {
  return (
    <section className={styles.SearchCategories}>SearchCategories</section>
  );
};

export default SearchCategories;
